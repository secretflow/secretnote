{
  "name": "telemetry",
  "private": true,
  "version": "0.0.8",
  "type": "module",
  "scripts": {
    "build:py": ":",
    "build:py_": "rye build --out ./dist",
    "publish:py": "python -m hatch publish ./dist",
    "build": "nx build:py"
  }
}
