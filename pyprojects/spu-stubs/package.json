{
  "name": "spu-stubs",
  "private": true,
  "files": [],
  "version": "0.0.0",
  "scripts": {
    "build:hatch": "python -m hatch clean && python -m hatch build -t sdist -t wheel",
    "format:black": "python -m black --check src",
    "lint:ruff": "python -m ruff check src",
    "publish:hatch": "python -m hatch publish ./dist",
    "typecheck:pyright": "pyright --project ../.. src"
  }
}
