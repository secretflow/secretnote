{
  "name": "@secretflow/notebook-pyproject-spu-stubs",
  "private": true,
  "files": [],
  "version": "0.0.0",
  "scripts": {
    "build": "hatch clean && hatch build -t sdist -t wheel",
    "test:pytest": "pytest",
    "lint:black": "black --check src tests",
    "lint:ruff": "ruff check src tests",
    "lint:pyright": "pyright src",
    "release": ": hatch publish ./dist"
  },
  "dependencies": {
    "@secretflow/py-spu-stubs": "workspace:^"
  },
  "nx": {
    "$schema": "https://cdn.jsdelivr.net/npm/nx@16.7.0/schemas/project-schema.json",
    "targets": {
      "build": {
        "inputs": [
          ":default",
          ":python"
        ]
      }
    }
  }
}
