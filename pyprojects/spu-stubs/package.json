{
  "name": "spu-stubs",
  "private": true,
  "files": [],
  "version": "0.0.1-dev.0",
  "scripts": {
    "build": "hatch clean && hatch build -t sdist -t wheel",
    "test:pytest": "pytest",
    "lint:black": "black --check src",
    "lint:ruff": "ruff check src",
    "lint:pyright": "pyright src",
    "release": "hatch publish ./dist"
  }
}
