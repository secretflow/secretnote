{
  "name": "secretnote",
  "private": true,
  "version": "0.0.8",
  "type": "module",
  "scripts": {
    "dev:sf": "cd .. && NODE_ENV=development python -m secretnote sf ./.secretnote --config=./secretnote/sf/.jupyter/config_dev.py --no-browser",
    "dev:scql": "cd .. && NODE_ENV=development python -m secretnote scql ./.secretnote --config=./secretnote/scql/.jupyter/config_dev.py --no-browser",
    "format:black": "python -m black --check src tests",
    "test:pytest": "python -m pytest",
    "typecheck:pyright": "pyright --project ../.. src/secretnote/server tests",
    "build:py": "rye build --out ./dist",
    "build:py:ci": "python -m build --out ./dist",
    "publish:py": "python -m hatch publish ./dist"
  },
  "dependencies": {
    "@difizen/libro-analyzer": "^0.1.0"
  }
}
